bundle:
  name: demo_bundle

include:
  - resources/*.yml

workspace:
  root_path: /Repos/Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}

targets:
  dev:
    default: true
    
  prod:
    workspace:
      root_path: /Repos/Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
